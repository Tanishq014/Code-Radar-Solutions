#include <stdio.h>

int isPrime(int num)
{
    if(num==1){printf("0");}
    int iss=0;
    else
    {
        
        for(i=2;i<=n;i++)
        {
            if(n%i==0){iss=1}
        }
    }return iss;
}